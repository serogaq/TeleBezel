# Security

Report vulnerabilities privately through GitHub's security advisory feature.
Do not include live credentials in reports. Rotate an exposed public API token,
internal service token, PostgreSQL password, or Laravel application key and
review logs immediately. The credential-sensitive TDLib volume must be protected
with the same care as a logged-in Telegram client.
